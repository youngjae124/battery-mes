# Copy this file to .env and fill in real values for local/production use.
# All variables have safe dev defaults baked into docker-compose.yml / application.yml,
# so .env is optional for local development but required before deploying with real secrets.

JWT_SECRET=
DB_USERNAME=
DB_PASSWORD=
ORACLE_PASSWORD=
